apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-200236-646w-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-200236-646w.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-200236-646w-service
            port:
              number: 80
